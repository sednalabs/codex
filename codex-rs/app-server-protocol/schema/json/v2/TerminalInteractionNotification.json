{
  "$defs": {
    "TerminalWaitInfo": {
      "properties": {
        "heartbeatIntervalMs": {
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "maxWaitMs": {
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "primitive": {
          "$ref": "#/$defs/TerminalWaitPrimitive"
        }
      },
      "required": [
        "primitive"
      ],
      "type": "object"
    },
    "TerminalWaitPrimitive": {
      "enum": [
        "execCommandWaitUntilTerminal",
        "writeStdinWaitUntilTerminal",
        "writeStdinEmptyPoll"
      ],
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "itemId": {
      "type": "string"
    },
    "processId": {
      "type": "string"
    },
    "stdin": {
      "type": "string"
    },
    "terminalWait": {
      "anyOf": [
        {
          "$ref": "#/$defs/TerminalWaitInfo"
        },
        {
          "type": "null"
        }
      ]
    },
    "threadId": {
      "type": "string"
    },
    "turnId": {
      "type": "string"
    }
  },
  "required": [
    "threadId",
    "turnId",
    "itemId",
    "processId",
    "stdin"
  ],
  "title": "TerminalInteractionNotification",
  "type": "object"
}
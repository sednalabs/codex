{
  "$defs": {
    "TerminalWaitPrimitive": {
      "enum": [
        "execCommandWaitUntilTerminal",
        "writeStdinWaitUntilTerminal",
        "writeStdinEmptyPoll"
      ],
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "heartbeatIntervalMs": {
      "format": "uint64",
      "minimum": 0,
      "type": [
        "integer",
        "null"
      ]
    },
    "maxWaitMs": {
      "format": "uint64",
      "minimum": 0,
      "type": [
        "integer",
        "null"
      ]
    },
    "primitive": {
      "$ref": "#/$defs/TerminalWaitPrimitive"
    }
  },
  "required": [
    "primitive"
  ],
  "title": "TerminalWaitInfo",
  "type": "object"
}

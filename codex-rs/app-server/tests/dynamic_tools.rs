// Dedicated integration-test binary for the native dynamic-tool registration
// seam so targeted validation does not need to compile the entire aggregated
// app-server integration suite.
#[path = "suite/v2/dynamic_tools.rs"]
mod dynamic_tools;

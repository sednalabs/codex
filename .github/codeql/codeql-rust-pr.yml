name: codex-codeql-rust-pr

queries:
  - uses: ./.github/codeql/rust-computer-use-contract

# Note: retained for planner/config consistency. Rust contract queries do not currently rely on threat-models.
threat-models: local

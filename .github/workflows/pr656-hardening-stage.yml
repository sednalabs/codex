name: Stage PR656 diagnostic hardening

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'diag/pr656-hardening-stage-20260821' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: diag/pr656-hardening-stage-20260821
          fetch-depth: 0

      - name: Verify staged source
        shell: bash
        run: |
          git diff --exit-code 783d6fbe19d45502df98378ae0e73b36e8f6f6aa -- \
            codex-rs/core/src/agent/control/spawn.rs \
            codex-rs/core/src/diagnostic_flags.rs \
            codex-rs/core/src/session/turn.rs \
            codex-rs/core/src/session/turn_tests.rs \
            codex-rs/core/src/tools/handlers/multi_agents_v2.rs \
            codex-rs/core/src/tools/handlers/multi_agents_v2/spawn.rs \
            codex-rs/ext/goal/src/extension.rs \
            codex-rs/ext/goal/src/runtime.rs \
            codex-rs/ext/goal/src/steering.rs

      - name: Apply lint cleanup
        shell: bash
        run: |
          python3 .github/scripts/pr656_hardening_stage.py
          cargo fmt --manifest-path codex-rs/Cargo.toml --all
          git diff --check

      - name: Run focused tests
        shell: bash
        run: |
          cargo test --manifest-path codex-rs/Cargo.toml -p codex-core goal_diagnostic_ --lib

      - name: Run repository argument-comment lint
        uses: ./.github/actions/run-argument-comment-lint
        with:
          target: Linux

      - name: Commit validated staged source
        shell: bash
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add \
            codex-rs/core/src/agent/control/spawn.rs \
            codex-rs/core/src/diagnostic_flags.rs \
            codex-rs/core/src/session/turn.rs \
            codex-rs/core/src/session/turn_tests.rs \
            codex-rs/core/src/tools/handlers/multi_agents_v2.rs \
            codex-rs/core/src/tools/handlers/multi_agents_v2/spawn.rs \
            codex-rs/ext/goal/src/extension.rs \
            codex-rs/ext/goal/src/runtime.rs \
            codex-rs/ext/goal/src/steering.rs
          git commit -m "test: make diagnostic telemetry lint-clean"
          git push origin HEAD:diag/pr656-hardening-stage-20260821

name: CI planner assertion maintenance (retired)

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "One-shot assertion maintenance completed; workflow retained inert only until cleanup."

name: CI planner fixture maintenance (retired)

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "One-shot maintenance completed; workflow retained inert only until cleanup."

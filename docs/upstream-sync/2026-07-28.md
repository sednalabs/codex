# Upstream harvest snapshot — 2026-07-28

> `status: current` · `authority: evidence` · owner: downstream maintainers

This dated snapshot records the upstream state and adoption decisions used by
the documentation-remediation train. It is deliberately bounded; it does not
pretend to settle the entire 60-commit upstream gap.

## Exact refs

- `origin/main`: `bff348fd68a99e1996d00dce1d46ba8ed9d37be3`
- `upstream/main`: `7cde2323f3712999e9ab98b16287e08b7735d52f`
- `origin/upstream-main`: `3418498f01422f5f650ea645d4bd19e05c3a9616`
- merge base: `a4535884169be8da2f81b8a4debecbd4dc11aa97`
- `origin/main...upstream/main`: `2131` downstream-ahead / `60` upstream-ahead
- `origin/upstream-main...upstream/main`: `0` mirror-ahead / `14` upstream-ahead

The maintained mirror is stale by 14 fast-forwardable upstream commits. The
audit therefore used the live upstream ref as its comparison basis and records
the mirror lag rather than calling it exact. The stable audit receipt was
captured at `2026-07-28T13:37:40Z`.

## Adoption table

| Ref                | Subject                                         | Decision     | Follow-up                                                                                                                       |
| ------------------ | ----------------------------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------- |
| `829f5b6b59`       | `protocol: separate app and exec RPC ownership` | Track        | Review during the next app-server code harvest; no docs-only schema adoption.                                                   |
| `0a0d09ad21`       | `Clarify docs folder guidance in AGENTS.md`     | Port concept | Adopted as repository-neutral authority and freshness guidance in [`../sedna-docs-governance.md`](../sedna-docs-governance.md). |
| `67849d950d`       | `Remove local docs and specs`                   | Ignore       | Downstream carry docs are an intentional evidence surface and remain required.                                                  |
| upstream PR #30866 | Reconcile loaded thread history on resume       | Track        | Adjacent to session identity and fork lineage; no runtime change in this train.                                                 |
| upstream PR #31487 | Installed runtime snapshot API                  | Track        | Revisit with the release-channel/runtime documentation work.                                                                    |
| upstream PR #31515 | Client-only web-search result metadata          | Track        | Revisit with the next app-server protocol harvest.                                                                              |

## Audit invocation and result

```text
python3 scripts/downstream-divergence-audit.py --repo . \
  --downstream-ref origin/main --mirror-ref upstream/main \
  --upstream-remote upstream --upstream-branch main \
  --registry-path docs/divergences/index.yaml \
  --output-dir /tmp/sedna-divergence-audit-current \
  --format both --code-only --enforce-registry
```

The pre-change receipt identified `scripts/pyproject.toml` as the sole
uncovered live code path. This train adds it to `ci-workflow-automation`; the
command must be rerun after the patch and its post-change receipt retained by
the operator.

## Scope note

The daemon installer/update-channel discrepancy remains a separate priority-0
runtime remediation. This snapshot records it so a later harvest cannot miss
it, but it does not claim that code or release behavior has been fixed here.
